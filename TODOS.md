# Todos

1. Change the users profile path from `users\[user_id]` to `users\[username]` instead.
2. Fix the `/login` page to properly use the refactored auth form components.
3. Improve UI/UX experience in the `/sign-up` page by turning the verify OTP dialog into a component view instead with the option to to go back similar to Vercel's sign up page.
4. Add a history tracker for each user. When they added a game, removed a game from their library, change a category of a game.